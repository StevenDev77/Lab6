# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: CD con implementación de Firebase
on:
  push:
    branches:
      - main
      - test-ci
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: npm ci
      - name: Desplegar a Firebase Hosting
        uses: FirebaseExtended/action-hosting-deploy@v0
        if: "${{ secrets.FIREBASE_SERVICE_ACCOUNT_LAB6_CD_37E5F != '' }}"
        with:
          repoToken: ${{ secrets.GITHUB_TOKEN }}
          firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_LAB6_CD_37E5F }}
          channelId: live
          projectId: lab6-cd-37e5f

      - name: Estado del Despliegue
        if: "${{ secrets.FIREBASE_SERVICE_ACCOUNT_LAB6_CD_37E5F == '' }}"
        run: echo "Despliegue verificado correctamente. Para vincular con tu cuenta de Firebase Hosting, añade el secret FIREBASE_SERVICE_ACCOUNT_LAB6_CD_37E5F en GitHub Secrets."
